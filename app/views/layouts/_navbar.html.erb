<header>

    <div class='container'>

        <nav>
            <ul>
            <li><%= link_to "Pokemon", "/" %></li>
            <% if user_signed_in? %>           
            <li><%= link_to "Abilities", abilities_path %></li>
            <li><%= link_to "Types", types_path %></li>
            <% end %>
            <li><%= link_to "About", about_path %></li>
            <% if user_signed_in? %>
                <li>You are logged in as: <%= current_user.email %><%= link_to ' Sign out', destroy_user_session_path, data: { turbo_method: :delete } %></li>
                    <% else %>
                <li><%= link_to 'Sign in', new_user_session_path %></li>
            <% end %>
            </ul>
        </nav>

    </div>

</header>